import { Button } from 'react-bootstrap';
import '../style/ViewClients.css';

function ViewClients() {
  return (
    <>
      <Button id="viewClients" variant="primary" size="lg">
        View Clients
      </Button>
    </>
  );
}

export default ViewClients;
